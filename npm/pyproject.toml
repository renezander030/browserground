[project]
name = "browserground-runtime"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
  "torch>=2.4",
  "torchvision",
  "transformers>=4.55,<4.60",
  "peft>=0.13",
  "huggingface_hub>=0.26",
  "pillow",
  "safetensors",
]
